/* *
 * Virtual area server
 * 
 * Controls the virtual area by creating rooms on demand, clean up, and other functions
 * 
 * @author {your name}
 * @created {date}
 * @typeof include
 * @doc /doc/build/virtual/generic_virtual
 */
#include <std.h>

inherit GENERIC_VIRTUAL;

void create() 
{
   ::create();
{wizard}
}