varargs int present_clones(string s, object container)

DESCRIPTION
Returns the number of clones present in container

The first argument is the full path to object
The second argument is optional and is the environment to check, if not found it will use previous object


EXAMPLES
present_clones("/std/obj/torch"); will return 0 or 1 if environment is calling object
present_clones("/std/obj/torch", this_player()); will return 0 or 1 if environment is this_player

LOCATION
/adm/simul_efun/misc.c

SEE ALSO
clone_max()
